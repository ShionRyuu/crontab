%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% @doc
%%% @copyright
%%% @end
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% Module declaration ===============================================
-module(crontab_app).
-behaviour(application).

%% Exports ==========================================================
-export([
    start/2,
    stop/1
]).

%% Code =============================================================
start(normal, Args) ->
    crontab_sup:start_link(Args).

stop(_S) ->
    ok.
